// Setup variables, input pin numbers, etc.

void setup()
{
  // Should setup a button and a light/uv sensor.
  // Also a thermometer (on the inside on specific room).
}

void loop()
{
  // Check if there's light out. (ASSUMES HAVING LIGHT WARMS UP
  // Check what's the inside temperature.
  // If there is, check to what temperature the system is set to.
  // if(temp < optimalTemp && !windowOpen)
    // open();
  //}
  // if(temp > optimalTemp && windowOpen) {
    // close();
  //}
  // if no light, then do the opposite.
}

// void close() {
  //close windows.
  //windowOpen = false;
//}

// void open() {
  //open windows.
  //windowOpen = true;
//}
